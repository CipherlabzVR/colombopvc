export const metadata = {
  title: "My Profile | Colombo PVC Center",
  description: "Update your account details for Colombo PVC Center.",
};

export default function ProfileLayout({ children }) {
  return children;
}
